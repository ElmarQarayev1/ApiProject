@model RoseEditRequest
@{
    ViewData["Title"] = "Edit";
}

<style>
    .img-box {
        margin-right: 10px;
        margin-bottom: 10px;
        display: inline-block;
        position: relative;
    }

    .remove-img-icon {
        position: absolute;
        top: 5px;
        right: 5px;
        cursor: pointer;
        color: red;
        font-weight: bold;
        font-size: 18px;
        background-color: white;
        border: 1px solid red;
        padding: 5px;
        border-radius: 50%;
    }
</style>

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">Roses</h1>
    </div>
    <div class="row">
        <div class="col-md-8 mx-auto">
            <form class="user" asp-action="Edit" method="post" enctype="multipart/form-data">
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" placeholder="Enter Name">
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group col-md-6">
                        <label asp-for="Desc"></label>
                        <input asp-for="Desc" class="form-control" placeholder="Enter Description">
                        <span asp-validation-for="Desc" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="Value"></label>
                    <input asp-for="Value" class="form-control" placeholder="Enter Value">
                    <span asp-validation-for="Value" class="text-danger"></span>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label asp-for="CategoryIds">Categories</label>
                        <select asp-for="CategoryIds" class="form-control" asp-items="@(new SelectList(ViewBag.Categories, "Id", "Name"))" multiple></select>
                        <span asp-validation-for="CategoryIds" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label for="venue">DiscountPercent</label>
                    <input asp-for="DiscountPercent" class="form-control" id="venue" placeholder=" " />
                    <span class="text-danger" asp-validation-for="DiscountPercent"></span>
                </div>
                <div class="form-group">
                    <label for="venue">DiscountExpireDate</label>
                    <input asp-for="DiscountExpireDate" class="form-control" id="venue" placeholder=" " />
                    <span class="text-danger" asp-validation-for="DiscountExpireDate"></span>
                </div>
                <label for="imgInput">Image File</label>
                <input type="file" name="Files" class="form-control imgInput" multiple>

                <div class="preview-box"></div>

                @if (ViewBag.Pictures != null)
                {
                    foreach (PictureResponse item in ViewBag.Pictures)
                    {
                        <div class="img-box mt-3 mr-3" style="display:inline-block;position:relative">
                            <span style="position: absolute;
                                  right: -9px;
                                  top: -22px;
                                  font-size: 28px;
                                  color: red;
                                  cursor: pointer" class="remove-img-icon">x</span>
                            <input type="hidden" name="ExistPictureIds" value="@item.Id" />
                            <img width="200" src="@item.Url" />
                        </div>
                    }
                }
                <button type="submit" class="btn btn-primary mt-3">Edit Rose</button>
            </form>
        </div>
    </div>
</div>
@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")

    <script>
        $(document).ready(function () {
            $(".imgInput").change(function (e) {
                let box = $(this).parent().find(".preview-box");
                $(box).find(".img-box").remove();

                for (var i = 0; i < e.target.files.length; i++) {
                    let imgBox = document.createElement("div");
                    imgBox.classList.add("img-box");

                    let removeIcon = document.createElement("span");
                    removeIcon.classList.add("remove-img-icon");
                    removeIcon.textContent = "x";
                    removeIcon.onclick = function () {
                        $(this).parent().remove();
                    };

                    let img = document.createElement("img");
                    img.style.width = "200px";
                    img.classList.add("previewImg");

                    let reader = new FileReader();
                    reader.readAsDataURL(e.target.files[i]);
                    reader.onload = () => {
                        img.setAttribute("src", reader.result);
                        imgBox.appendChild(img);
                        imgBox.appendChild(removeIcon);
                        box.append(imgBox);
                    }
                }
            });

            $(".remove-img-icon").click(function () {
                $(this).parent().remove();
            });
        });
    </script>
}
